/**
 * @murihspace/types
 *
 * This package will store shared TypeScript types and API request/response
 * contracts for MurihSpace applications.
 *
 * Shared interfaces and type guards will be added as API contracts are defined
 * in later feature development tasks.
 */

// Placeholder interface to represent empty module exports
export interface MurihSpaceTypesPlaceholder {
  version: string;
}
